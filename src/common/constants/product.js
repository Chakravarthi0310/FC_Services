const productUnits = {
    KG: 'kg',
    DOZEN: 'dozen',
    PIECE: 'piece',
};

const productUnitNames = Object.values(productUnits);

const categoryNames = ['Vegetables', 'Fruits', 'Dairy', 'Grains'];

module.exports = {
    productUnits,
    productUnitNames,
    categoryNames,
};
